# Оставьте пустым или укажите /api — тогда Vite проксирует на Django (см. vite.config.js)
# VITE_API_BASE_URL=/api

# Либо полный URL, если без прокси (нужен CORS на backend):
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
